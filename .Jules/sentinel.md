## 2024-05-24 - Ruff Check Violations\n**Vulnerability:** Found EXE001 (executable permissions on shebang files) and BLE001 / S110 (blind exceptions) failing CI.\n**Learning:** CI strictly enforces executable bit on files starting with #! and disallows 'except Exception: pass'.\n**Prevention:** Use git update-index --chmod=+x for python scripts and handle/log exceptions properly instead of catching blind exceptions.
## 2024-05-24 - Ruff Check Violations 2\n**Vulnerability:** Found BLE001 (blind exception) failing CI on 'except Exception as e:' even though the exception was logged.\n**Learning:** Ruff BLE001 rule requires either catching specific exceptions or adding a '# noqa: BLE001' comment when catching a general Exception is necessary and handled (e.g. at the top-level main entrypoint).\n**Prevention:** Use '# noqa: BLE001' or catch specific exceptions.
