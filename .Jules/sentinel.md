## 2024-05-24 - Ruff Check Violations\n**Vulnerability:** Found EXE001 (executable permissions on shebang files) and BLE001 / S110 (blind exceptions) failing CI.\n**Learning:** CI strictly enforces executable bit on files starting with #! and disallows 'except Exception: pass'.\n**Prevention:** Use git update-index --chmod=+x for python scripts and handle/log exceptions properly instead of catching blind exceptions.
