## 2024-05-17 - Actionable Empty States
**Learning:** Empty states in CLIs often just state the obvious ("no channels"). This is a missed opportunity to guide the user.
**Action:** Always provide a clear, inline call-to-action in empty state outputs (e.g., "use 'init' to create one" instead of just "no channels").
